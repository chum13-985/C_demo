#include <stdio.h>

int main()
{
    int i, n, sum=0;

    printf("enter the n terms of even numbers: ");
    scanf("%d", &n);

    for(i=2; i<=n; i+=2)
    {
       
        sum += i;
    }

    printf("Sum of all even number  = %d",  sum);

    return 0;
